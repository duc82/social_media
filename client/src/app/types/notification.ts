export interface NotificationSettings {
  id: string;
  likes: boolean;
  comments: boolean;
  friendRequests: boolean;
  birthdays: boolean;
  events: boolean;
  groups: boolean;
  messages: boolean;
}
